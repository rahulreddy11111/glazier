import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    time_frame: {
      height: '85%',
      width: '30%',
      backgroundColor: '#0f8185',
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius : 20,
      borderWidth : 0.2,
      borderColor : '#fff'
    },
});

export default styles;